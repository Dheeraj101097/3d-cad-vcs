# Copy to .env.local for local dev (leave blank — Vite proxy handles /api)
VITE_API_URL=

# Set this to your Render backend URL in Vercel's environment variables:
# VITE_API_URL=https://your-app-name.onrender.com
